# 환경 변수 및 보안 파일
.env
# .env 원자적 교체 중 만들어지는 임시 파일 (토큰이 담겨 있음)
.env.tmp.*

# 파이썬 캐시 파일
__pycache__/
*.py[cod]
*$py.class

# 가상 환경
venv/
.venv/
env/

# 에디터 설정
.vscode/
.idea/

# 실행 중 생성되는 상태/로그 파일 (서버와 로컬이 각자 따로 유지해야 함 — git 충돌 방지)
seen_ids.json
*.pid
nohup.out
.last_startup_notice
